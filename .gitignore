# Build output
build/

# Local user overrides (copy user.mk.example -> user.mk)
user.mk

# VS Code workspace settings (if any local overrides are created)
.vscode/*.code-workspace

# Avoid committing proprietary J-Link binaries by default
/environment/jlink/*.exe
/environment/jlink/*.dll
/environment/jlink/*.zip
